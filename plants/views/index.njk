<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/styles.css" />
  <link rel="stylesheet" href="/semantic.css" />
  <title>Plantly</title>
</head>
<body>
  <header>
    <h1>Plantify</h1>
    <nav>
      <ul>
        <img src="green.png">
        <li><a href="#">Home</a></li>
        <li><a href="#">About</a></li>
        <li><a href="#">Contact</a></li>
      </ul>
    </nav>
  </header>
  
  <main class="main-layout">
    <aside class="filters">
      <h3>Filters</h3>
      <div>
        <label>Filter By</label>
        <ul>
          <li><input type="checkbox" /> Data</li>
          <li><input type="checkbox" /> Type</li>
          <li><input type="checkbox" /> More Filters Coming Soon</li>
        </ul>
      </div>
    </aside>
    
    <section class="plant-listing">
      <form id="plant-form">
        <label for="plant-search">Search plants by name:</label>
        <input type="text" id="plant-search" name="plant" placeholder="Enter plant name" required />
        <button type="submit">Search</button>
      </form>
      <ul id="plants-container" class="plants-container">
        {% for plant in plants %}
       <div class="plant-card">
  <div class="card-inner">
    <!-- Front side of the card -->
    <div class="card-front">
      <h3 class="plant-name">{{ plant.common_name }}</h3>
      <img src="{{ plant.image_url }}" alt="{{ plant.common_name }}" class="plant-image" />
    </div>
    <!-- Back side of the card -->
    <div class="card-back">
      <h3 class="plant-name">{{ plant.scientific_name }}</h3>
      <p>{{ plant.family }}</p>
       <p>{{ plant.slug }}</p>
        <p>{{ plant.year }}</p>
      <button class="add-to-watchlist">❤️</button>
      <button class="add-to-cart">🛒</button>
    </div>
  </div>
</div>
        {% endfor %}
      </ul>
    </section>
    
    <aside class="watchlist">
      <h3>Watchlist</h3>
      <p id="my-collection">Your collection will appear here...</p>
    </aside>
  </main>

  <footer>
    <p>home | about | contact</p>
  </footer>
  
  <script src="script.js" type="module"></script>
</body>
</html>
